import 'package:flutter/material.dart';

class Fromscreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text("แบบฟอร์มบันถึกข้อมูล"),
        ),
        body: Container());
  }
}
